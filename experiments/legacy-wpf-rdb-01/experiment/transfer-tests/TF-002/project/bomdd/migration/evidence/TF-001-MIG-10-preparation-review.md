# TF-001 MIG-10 Preparation Review

The transfer-test organizer copied the frozen Traggo baseline document, build log, DB document, schema, DB observation, three UI screenshots, one DOM extract, baseline fixture and retained restore-control copy into the isolated project.

Only `ART-BASELINE` and `ART-BUILD-LOG` are accepted by the organizer. The fresh worker must close STEP-013 and STEP-014, construct and validate both semantic manifests, accept `ART-DB-BASELINE` and `ART-CURRENT-OBS`, obtain the three assigned-role approvals, pass MIG-10 and write a handoff.

