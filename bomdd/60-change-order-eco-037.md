# Change Order — ECO-037(calibrate の判定帰結と入力クラス被覆 — ECO-035 延長収束レビューの指摘 2 件)

> 裁定: user 2026-08-31「延長として実行して」— ECO-035 `/converge` ループの延長
> (ECO-036 改訂後の手順 5「延長は人間裁定のみ」の初適用・上限 +3 周)。
> **gate ① 承認済み 2026-08-31 maintainer(3 点裁定 — 下記 gate ① 節)。**

## 担当設備(equipment)

- 設計(延長レビュー実施):
  - requested: `claude-fable-5`
  - resolved: `claude-fable-5`
  - ハーネス: Claude Code(Claude Agent SDK)
  - 来歴: **self-reported**
- 検査官: なし(独立検査なし)

## 0. 実測(起票根拠・2026-08-31)

ECO-035 の設計ループは round 3=1 の収束失敗のまま打ち切られていた(同 order receipt)。
延長 3 周の実測:

- **round 4 = 2 件**(下記 F4-1 / F4-2)。
- **round 5 = 0 件 / round 6 = 0 件 — 2 周連続ゼロで収束成立**(通算軌跡: 3→2→1→2→0→0)。
- 副次実測(指摘ではない): 引用 §6.4 の `harness_bug` 帰属は実文と一致(playbook:369)/
  表 A 全 10 行の初出来歴を一次記録(playbook §4.4・register・CLAUDE.md・ECO-038 還元
  コミット)と突合し全行一致。

### F4-1【欠陥】: 3 値判定「証明していない」に帰結が未規定

`calibrate.md` 手順 4 は各主張へ 3 値(証明している / 部分的 / 証明していない)を付けさせるが、
「部分的」には帰結がある(測っていない次元を昇格先の記録に残す)のに対し、
**「証明していない」には何の帰結も規定されていない** — 判定を receipt に書けば、証明して
いない証拠を根拠にした昇格がそのまま通る。ECO-036 が converge 側で是正した「帰結のない宣言」
(converge 表 A ⑧ 未収束の無帰結化)と同一クラスであり、その是正の**翌日に同型を新設スキルへ
埋めていた**ことになる。

### F4-2【被覆ギャップ】: battery に入力の意味クラス被覆の行がない

playbook §4.4 同等性受入(playbook:143-149)は「順位等価・集計指標はランダム誤差を吸収するが、
**系統誤差は特定の入力クラスの体験だけを静かに劣化させる**」と規定し、入力クラス別の対照群を
要求する(実測: 経路非対称で系統的 -18 点が潜伏)。calibrate の battery Q1〜Q10 は契約軸の
被覆(Q3)は問うが、**入力クラス軸**を問う行がない — 教義の index として穴。

## 1. 変更要求(calibrate 正本+写しの同一改訂)

### (a) 手順 4 へ「証明していない」の帰結を追加【製造対象・F4-1 是正】

追記案: 「**証明していない**証拠は昇格の根拠に使わない — 証拠を補う / 主張を弱めて記録する /
裁定に上げる、のいずれかを選ぶ(判定に帰結を持たせる)。」

### (b) battery へ Q11 追加【裁定対象・F4-2 是正】

| # | 問い | 正本 |
|---|---|---|
| Q11 | 入力の**意味クラス別**に測ったか — 集計・順位の緑はクラス別の系統誤差を吸収して隠す | §4.4 同等性受入 |

推奨= 採択(battery は「該当する行だけを選ぶ」index であり、行の追加は適用コストを増やさない)。

### (c) 検討して不採: 較正 receipt への起動経路フィールド追加

converge は ECO-036 で起動経路を receipt へ追加したが、calibrate は判断待ち停止機構を
持たないため起動経路の消費者が現状いない — §8.5 に従い必要の実測後(対称性だけを理由に
様式を増やさない)。

## gate ①(製造承認)

**承認 2026-08-31 maintainer** — 3 点裁定:

1. **F4-1 是正: approve**。「『証明していない』証拠は、その主張の昇格根拠として使用しては
   ならない。帰結は、証拠追加 / 主張縮小 / 必要時の裁定上申とする。『証明していない』自体を
   全体 FAIL とは扱わない」— 起票案の帰結 3 肢を採択し、**主張単位で閉じる**(査定全体を
   FAIL させない)ことを裁定で精密化。
2. **Q11 追加: approve(範囲を限定)**。「対象は『全入力の意味分類』ではなく、**測定器の
   系統誤差に関係する意味的に異なる入力クラスの被覆確認**とする。use/mention 等の既知実例を
   最低 1 件アンカーとして持つ」— 起票案(§4.4 同等性受入のみ)より対象を計器の系統誤差に
   絞り、既知実例アンカー(use/mention = BomDD ECO-034)を行内に必須化。
3. **較正 receipt への起動経路フィールド: 不採を維持**。「消費者・停止機構の必要性が実測
   された時点で再起票する」。

## スコープ外(宣言済み境界)

- ECO-035 の記録(receipt 含む)への遡及改訂(append-only 追記のみ実施済み)。
- calibrate の効果測定・非起動観測(ECO-035 スコープ外宣言が生きている)。

## 2. 影響なし予測(反証可能・製造前に凍結)

- diff は **calibrate 正本+写し+台帳系**のみ:
  `method/templates/product-profile/skills/calibrate.md` / `.claude/skills/calibrate/SKILL.md`。
- **C4 判定不変**(プレースホルダー増減なし)。**C7 判定不変**(SKILLS 実数 10 のまま)。
  **C13 判定不変**(markdown リンク増減なし)。**C16 は本 order を required と判定する見込み**
  — receipt を下記に埋め込む。既設製品リポへは kit 再設置まで非波及。

## 3. 受入(起票時凍結)

- **V1**: 正本⇔写しの内容同期を機械突合(ECO-036 のプローブ再用・正規化後の残差 0 行)。
- **V2**: `self-conformance` 全検査 PASS・既存判定不変。
- **V3**: push 後 CI 緑(4 値判定・headSha 照合)。
- **V4**: diff が影響なし予測の窓内(2 ファイル+台帳系)に収まる。
- 較正: 散文規則につき機械対照なし(ECO-036 と同じ正直宣言)— 帰結規則(a)の遵守は
  較正 receipt の実運用で観測する。

## /converge receipt(延長ループ — 新様式)

- **判定: 収束**(延長 round 軌跡: 2→0→0 — 2 周連続ゼロ成立。ECO-035 通算: 3→2→1→2→0→0)。
- **起動経路: 人間裁定による延長**(user 2026-08-31「延長として実行して」— 手順 5
  「延長は人間裁定のみ」の初適用。自発の周回予算積み増しではない)。
- 周回数と新規指摘: round 4 = 2 件(F4-1 / F4-2)/ round 5 = 0 件 / round 6 = 0 件。
- 検証した主張(要点): §6.4 `harness_bug` の実在= playbook:369 実読 / §4.4 入力クラス
  系統誤差= playbook:143-149 実読 / 表 A 全 10 行の初出来歴= 一次記録と突合(§0)/
  ECO-035 の軌跡と receipt 実文= 同 order 実読 / 起票時 register の filed 様式=
  `git show 78af21b` 実測。
- 未収束事項: なし。ただし**収束が証明するのは設計の内部整合まで**であり、スキルの効果
  (計器欠陥の検出率)は依然未証明(運用実測の領分 — ECO-035 スコープ外宣言のとおり)。

## 4. 製造と受入の実測(2026-08-31)

- diff 監査の窓: baseline `52e600d`(起票コミット= 是正開始直前へ更新)→ head は受入時に確定。
- (a) 手順 4 へ帰結を追記 — gate 裁定の文言どおり: 昇格根拠としての使用禁止・帰結 3 肢
  (証拠追加 / 主張縮小 / 必要時の裁定上申)・**「証明していない」判定そのものは査定全体の
  FAIL ではない(主張単位で閉じる)**を明記。
- (b) battery へ Q11 追加 — gate 裁定どおり対象を「**計器の系統誤差に関わる意味的に異なる
  入力クラス**」に限定し、既知実例アンカー(use/mention クラス= 検査を論じる文書だけが
  系統的に誤判定された・BomDD ECO-034)を行内に明記。正本列= §4.4 同等性受入+BomDD ECO-034。
- (c) は不採維持(変更なし)— 消費者・停止機構の必要が実測された時点で再起票。
- 正本と写しへ同一適用。
- **V1 PASS**: 正本⇔写しの機械突合(ECO-036 プローブと同一の正規化)= 「同期成立 —
  正規化後の残差 0 行」・exit 0。
- V2〜V4 は §5 に記録。

### 較正 receipt(/calibrate 自己適用 — トリガー③: 検査器〔本スキル自身〕の変更直後)

- 査定した主張と 3 値判定:
  1. 「正本と写しは同期している」— **証明している**(V1 機械突合・残差 0 行)。
  2. 「本変更は既存の機械検査を壊さない」— **証明している**(V2 全 PASS・§5 実測)。
  3. 「Q11 の既知実例アンカーは実在する」— **証明している**(BomDD ECO-034 register 実文:
     use/mention 混同で cutoff 以降 3 件中 1 件= 検査を論じる文書クラスのみが誤判定)。
  4. 「帰結 3 肢は F4-1 の欠陥を閉じる」— **部分的**(規則としての欠落は閉じたが、遵守は
     機械検査されない — 測っていない次元として下記に宣言)。
- 検出した計器欠陥と帰属: なし(本弧のプローブ・ゲートとも所見なし)。
- 検出力の限界(実施した検査が測っていない次元): (1) 手順 4 の帰結 3 肢の遵守は散文契約で
  機械観測されない (2) Q11 の適用判断(どの入力クラスを「系統誤差に関係する」とみなすか)は
  査定者の裁量に残る — 既知実例アンカーはその較正点であり網羅ではない (3) 写し⇔正本の同期は
  一回性プローブ(恒久検査なし・ECO-035/036 と同じ限界)。

## 5. CI 実測(V3)

(push 後に追記)

## 6. クローズ

(受入後に追記)
