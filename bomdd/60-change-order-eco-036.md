# Change Order — ECO-036(converge 未収束の停止点 — 収束失敗の裁定点化・自動延長の禁止・自発起動の判断待ち停止)

> 裁定: user 2026-08-31「これは収束した、と言い切るのは誠実ではないかもしれません。たとえば、
> 3 回以内に収束しなかった。ここで打ち切るか、例えばもう 3 回追加するか?などの方が良さそうです。
> ただし、自動発動のケースなら、判断待ちとして停止でよいかもしれません」+ 同日
> 「ECO-036 として起票して製造まで進めてください」。

## 担当設備(equipment)

- 製造(設計者):
  - requested: `claude-fable-5`
  - resolved: `claude-fable-5`
  - ハーネス: Claude Code(Claude Agent SDK)
  - 来歴: **self-reported**
- 検査官: なし(独立検査なし)

## 0. 実測(起票根拠・2026-08-31)

- **ECO-035 弧の実測**: order 内の収束 receipt は「2 周連続ゼロ未到達のため収束は主張しない」と
  正直に記録していたが、(i) 完了報告は「/converge 起動下で実施・C16 が検査して PASS」と並べ、
  **収束したかのように読める**形だった(C16 が測るのは receipt の存在のみ —
  `method/tools/self-conformance.py:888`「receipt の構造的存在しか測らない」)
  (ii) 未収束宣言は**何の帰結も持たず**、自発起動+一括委任の弧でそのまま製造・受入・正典化
  まで進んだ。ECO-034 で欠陥と裁定した「出口のないゲート」の鏡像=「**帰結のない未収束宣言**」。
- **構造的観察**: 現行仕様(2 周連続ゼロで収束・上限 3 周)では、round 1 で指摘が 1 件でも
  出た時点で、収束成立には round 2・3 が両方ゼロである必要があり、実質ほぼ到達不能。
  実績も cutoff 以降の receipt 2/2 が未収束提示(ECO-033 order §7= round 1=3/2=2/3=1・
  ECO-035 order= round 1=3/2=2/3=1)。**現状の収束条件は判定にほぼ寄与せず、receipt は
  「周回した」ことの証明に留まっている**。
- 対象の正本は `method/templates/product-profile/skills/converge.md`(ECO-032 で BomDD が
  正本・`.claude/skills/converge/SKILL.md` は写し・同期規則あり)。

## 1. 変更要求(製造対象 — converge 正本と写しの同一改訂)

- **(a) 手順 5**: 上限 3 周に達して 2 周連続ゼロ未到達なら「**収束失敗**」であり、収束した
  かのように扱わない。以後は裁定事項 — **打ち切り採用(全部か一部かは裁定で指定)/ 延長 /
  設計の差し戻し**を round 軌跡(単調減少か横ばいか)を材料に人間へ問う。**延長は人間裁定
  のみ**(担当設備が自分で周回予算を積み増すと、抱え込み防止という上限の目的が消える)。
- **(b) 手順 6**: 未収束提示は receipt **冒頭**に判定「未収束(round 軌跡)」を置き、
  **報告文で収束と読める表現を使わない**(receipt の機械検査は存在しか測らない — 主張の
  誠実さは本文の責務)。**自発起動の弧では、未収束の設計を裁定が出るまで製造・正典化へ
  進めない(判断待ち停止)**。
- **(c) receipt 様式**: 冒頭へ**判定**(収束 / 未収束+裁定質問)と**起動経路**(人間呼び出し /
  自発)の 2 項を追加(既存 3 項は不変 — 後方互換・過去 receipt へは遡及しない)。
- **(d) 失敗型 表 A へ ⑧ 追加**: 「未収束の無帰結化」(初出= BomDD ECO-035)。
- **(e) 写しへの同期**(ECO-032 の同期規則どおり正本→写し)。

### 選択肢集合(散文改訂以外のレバー — 不採理由つき)

| 案 | 内容 | 不採理由 |
|---|---|---|
| B-1 | C16 拡張で未収束 receipt を機械検出し、後続 commit を遮断 | §8.5 — 散文規則の機械化は運用の実測後に判断(ECO-032→033 と同じ順序)。(c) の判定フィールドが機械可読の足場になるため、将来の拡張を妨げない |
| B-2 | 上限を撤廃して収束まで回す | 抱え込み防止という上限の目的が消える。収束しない論点は構造的に人間か追加情報が要る(round 軌跡が横ばいの場合) |

## gate ①(製造承認の来歴)

- **承認**: user 2026-08-31「ECO-036 として起票して製造まで進めてください」(冒頭引用の
  フィードバックが設計核そのもの — 裁定点化・延長の扱い・判断待ち停止はユーザー自身の提案)。

## スコープ外(宣言済み境界)

- **C16 の拡張**(未収束判定の機械検査・判断待ち停止の機械強制): §8.5 に従い運用実測後。
- **TimetableAdv 側の写し同期**: 別リポの ECO(ECO-032 スコープ外宣言が生きている)。
- **過去の receipt(ECO-033/035)への遡及改訂**: しない(歴史的記録の非消去)。

## 2. 影響なし予測(反証可能・製造前に凍結)

- diff は **converge 正本+写し+台帳系**のみ、他は diff ゼロ:
  `method/templates/product-profile/skills/converge.md` / `.claude/skills/converge/SKILL.md`。
- **C4 判定不変**(render の対象プレースホルダーは `{{METHOD}}` のみで増減なし)。
  **C7 判定不変**(SKILLS 実数 10 のまま)。**C13 判定不変**(両ファイルの markdown リンクは
  写し冒頭の正本参照 1 件のみで増減なし)。**C16 は本 order を required と判定する見込み**
  (hard-positive 語彙を含む)— receipt を埋め込む。既設製品リポへは kit 再設置まで非波及。

## 3. 受入(起票時凍結)

- **V1**: 正本⇔写しの内容同期を機械突合(プレースホルダー解決+写しヘッダー除去の正規化後、
  残差 0 行)。
- **V2**: `self-conformance` 全検査 PASS・既存判定不変。
- **V3**: push 後 CI 緑(4 値判定・headSha 照合)。
- **V4**: diff が影響なし予測の窓内(2 ファイル+台帳系)に収まる。
- **V5(新様式の初回使用)**: 本 order の収束 receipt 自身が新様式(冒頭判定・起動経路・
  round 軌跡・裁定質問)に従う — 新設規則の初回使用者は新設者自身(ECO-034/035 の前例)。

## /converge receipt(本 order の設計に適用 — 新様式の初回使用)

- **判定: 未収束(round 軌跡: 2→1→0・単調減少)**。2 周連続ゼロには round 4 = 0 が必要だが
  上限 3 周に到達。裁定質問(打ち切り採用 / 延長 / 差し戻し)→ **本弧は user の事前裁定
  「起票して製造まで進めてください」+設計核がユーザー自身の提案であることをもって
  打ち切り採用と扱う**(停止点は裁定済みにより充足 — 判断待ち停止は発動しない)。
- **起動経路: 自発**(対策仕様の設計合成を認識して適用。人間の /converge 呼び出しなし)。
- 周回数と新規指摘: round 1 = 2 件(receipt へ起動経路フィールドを追加 — 自発か否かを
  観測可能に / 打ち切り採用の「全部か一部か」を裁定肢に明記)/ round 2 = 1 件(報告文統制と
  判断待ち停止は機械検査されない旨を本文へ限界として明文化)/ round 3 = 0 件。
- 検証した主張(要点): C16 が存在のみ測る= `self-conformance.py:888` 実読 / cutoff 以降の
  receipt 2/2 が未収束= ECO-033 order §7・ECO-035 order の実文 / 正本と写しの所在・同期規則=
  ECO-032 order §1(d)・写し冒頭ヘッダー実読 / SKILLS・リンク数への非影響= 変更対象ファイルの
  実 diff。
- 未収束事項: 上記のとおり round 4 未実施(裁定済み打ち切りのため)。内容上の未解決論点は
  残っていないが、**収束の形式的成立は主張しない**。

## 4. 製造と受入の実測(2026-08-31)

- diff 監査の窓: baseline `31eaefe`(起票直前 HEAD)→ head は受入時に確定。
- (a)〜(d) を正本へ、(e) で写しへ同一適用(手順 5・6・receipt 節・表 A ⑧)。
- **V1 PASS**: 同期突合プローブ(scratchpad `eco036_sync.py`)= 「同期成立 — 正規化後の
  残差 0 行」・exit 0。正直記載: プローブ初版は成立判定の print が cp932 で落ちた
  (判定は成立側に到達済み・出力層の欠陥でプローブ帰属・UTF-8 明示で是正して再実行)。
- V2〜V4 は §5 に、V5 は本 order の receipt 自身(上掲)。

### 較正 receipt(/calibrate 自己適用 — トリガー③: 検査器〔converge 手順〕の変更直後)

- 査定した主張と 3 値判定:
  1. 「正本と写しは同期している」— **証明している**(V1 機械突合・残差 0 行)。
  2. 「本変更は既存の機械検査を壊さない」— **証明している**(V2 全 PASS・§5 実測)。
  3. 「新規則は未収束の無帰結化の再発を防ぐ」— **証明していない**(散文規則であり機械観測は
     ない — スコープ外宣言どおり運用実測で測る)。
- 検出した計器欠陥と帰属: ゲート側 0 件。プローブ側 1 件(V1 プローブ初版の出力
  エンコーディング — 判定前に是正・§4 に正直記載)。
- 検出力の限界(実施した検査が測っていない次元): (1) 報告文統制(「収束と読める表現を
  使わない」)と判断待ち停止は機械検査されない — C16 は receipt の存在のみを見る
  (2) 起動経路の申告は self-reported で裏取り機構がない (3) 写し⇔正本の同期は本 ECO の
  一回性プローブで、恒久検査ではない(ECO-035 と同じ限界)。

## 5. CI 実測(V3)

(push 後に追記)

## 6. クローズ

(受入後に追記)
