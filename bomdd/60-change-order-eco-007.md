# ECO-007 — 期待結果 manifest の失敗分類・シグネチャ対応(playbook §13 新規範への治具追随)

## 起票(2026-07-10)

- 出典: harness ECO-001〜006 還元のレビュー承認文面(メンバー・案A)。playbook §13 の期待結果
  manifest 規則が精密化された — 「manifest にはテスト識別子だけでなく、**期待する失敗分類および
  判定可能な失敗シグネチャ**を記録する。期待失敗の unexpected pass、**期待理由と異なる失敗**、
  新規の未登録失敗をいずれもゲート失敗とする」。
- 乖離: ECO-006 初版の manifest はテスト識別子のみ・C9 は二方向(unexpected pass/新規失敗)のみ。
  規範を本文へ織り込んだ時点で、実装が規範に不足する状態になる — 同日中に追随して乖離を残さない。

## 裁定

- 規範自体はレビュー承認済み(2026-07-10)。治具追随は規範織り込みの帰結として実施。
- signature の設計: trx の実 Message から**判定可能な部分文字列**を採取(B01= `[cv]null[vout]`・
  B10= `Expected: 2`・B11/B12= `Expected: 1`)。分類= `assertion-failure/L0-overcoupling`
  (loop-02-report の裁定に一致)。旧形(文字列リスト)は後方互換で受理。

## 影響分析(製造前凍結)

- 影響なし予測: manifest と self-conformance.py 以外は diff ゼロ。既存の二方向判定は不変。

## 検証(2026-07-10)

- 基線: `--dotnet` 全 PASS(loop-02 = 26/30・期待赤 4 件一致・**signature 突合 4 件**)。
- **変異テスト(三方向目の陽性対照)**: B01 の signature を偽値(`THIS-SIGNATURE-DOES-NOT-EXIST`)
  へ改変 → C9 が「**期待理由と異なる失敗(signature 不一致): [B01...]**」として FAIL(exit 1)。
  manifest 復元後に再度 PASS。
- 影響なし予測: 的中。

## 教訓

- **規範の精密化と実装の追随は同一変更窓で閉じる** — 本文が実装より先に進んだ状態を「還元完了」と
  呼ばない(規範→実装の fail-late を作らない)。ECO-005 教訓「被覆規則と適格性規則を別々に拡張しない」
  の規範/実装版。
