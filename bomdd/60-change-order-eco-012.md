# ECO-012 — 自己適合ゲートの YAML 検査が構文成功のみで情報保存を検査しない(重複キー沈黙 PASS・実台帳が走査対象外)

> 状態: **起票のみ(裁定・製造前)**。是正/検証は未着手 — gate(製造承認)で停止中。
> **gate は ECO-011 適用後**(順序依存 — 是正前の現台帳が本 ECO の天然の陽性対照)。

## 起票(2026-07-16)

- 出典: **保存形式レビュー(2026-07-16)所見1 の同根**。実害の実測= ECO-011
  (重複キーによる summary 誤帰属が C1/C3 全 PASS のまま 5 日潜伏: 2026-07-11〜07-16)。
- 現状の検査の穴(実コード確認済み・self-conformance.py):
  - **C1**(64〜76 行): `yaml.safe_load` のパース成功のみが合格条件。PyYAML の既定挙動は
    重複キーを**エラーにせず後勝ちで上書き** — 情報損失が構文 PASS で素通りする。
    さらに走査対象が `method/` に閉じ、**実台帳(bomdd/)は対象外**。
  - **C3**(91〜106 行): 実台帳を同じ `safe_load` で読む — 重複キー・情報損失を検出しない。
  - **C4**(scaffold 煙試験): 生成後 scaffold の YAML に構文検査なし(生成物の保存正本も同じ穴)。
- 方法論上の位置づけ: transfer-04 4類型の**「存在 vs 完全性」**(FINDINGS §11.4・silence §16)の
  再演 — 「パース結果が存在する」ことで「情報が保存されている」ことを代用していた。
  ゲート docstring「欠測・実行不能は「問題なし」ではない」の情報保存版。

## 裁定

- **未(gate 承認待ち)**。

### 是正方針案(製造前・凍結前の草案)

1. **厳格ローダーの共通化**: 重複キーで FAIL する SafeLoader 派生(構築時に key 集合を検査・
   重複でエラー位置つき例外)をヘルパ関数化し、C1/C3 の読込を差し替える。
2. **検査対象の拡張**:
   - C1 の走査に `bomdd/` の実台帳 YAML を追加(または C3 側で厳格ロード — 実装時に一本化)。
   - C4 の scaffold 煙試験で、生成された YAML 全件を厳格パース(生成物も保存正本)。
3. **(gate 採否)C3 拡張 — verified の完全性検査**: `status: verified` のエントリで
   `verification` が製造前プレースホルダ(「(製造前」を含む)なら FAIL。
   OBS-20260716-02(accept 段の部分更新様式)の機械面④ — rule of three 前の先行機械化に
   あたるため gate で採否裁定(実害 1 例=ECO-011 は実測済み)。
4. **変異テスト(陽性対照・playbook §13「検査の新設は陽性対照と対で」)**:
   - 変異 A: 一時コピーへ意図的な重複キーを注入 → C1(厳格化後)が FAIL。
   - 天然対照: ECO-011 是正**前**の台帳に対し新 C1 が FAIL・是正**後**に PASS
     (是正と検査の等価証明 — ECO-010 V5 と同じ様式)。
   - 変異 B(案 3 採用時): verified エントリの verification をプレースホルダへ偽装 → FAIL。

## 影響分析(製造前予測 — 未凍結)

- 影響なし予測: `method/tools/self-conformance.py` 以外 diff ゼロ。既存 C1〜C9 の正常判定は不変
  (ECO-011 適用後の台帳・テンプレ・生成 scaffold は重複キー 0 件を 2026-07-16 に実測済み)。
  exit code 体系(0/1/2)は不変。
- 順序依存: ECO-011 が未適用のまま本 ECO を適用すると、新 C1 が現台帳で FAIL する
  (それ自体は正しい検出だが、窓の分離のため ECO-011 先行とする)。

## 是正

- (未着手)

## 検証

- (未実施)

## 教訓(還元候補 — lesson-promote 経由)

- **保存正本の検査は「構文が通る」でなく「情報が保存される」を張る** — 寛容パーサの既定挙動
  (重複キー後勝ち・型強制等)は検査の暗黙前提になりやすい(ECO-079「暗黙前提 3 様態」の
  検査層版・OBS-20260716-01)。
