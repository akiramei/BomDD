§10 では、次の2つを同じ受入条件として扱っています。

1. 現行のすべての step 形を受理すること
2. `dist: swap to <rev>` を受理すること

しかし、この2つを同時に成立させると、装置の状態は `staged` のままなのに、`dist` の変更として扱う形になります。
この組み合わせは、これまでの履歴には存在しません。
また、今回採用した規則では、この状態は `dist swap` とはみなされません。実際には装置側の変更であり、それを `dist` の変更として表現しているだけです。