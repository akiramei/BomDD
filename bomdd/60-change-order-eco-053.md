# Change Order — ECO-053(C16/C17 receipt 検出の共通化+C16 本体ラベル・converge 手順 6 の補正・見逃しの記帳)

> 裁定: user 2026-09-02「a′+b′+d を採択して製造まで進めて」— converge スキル評価(人間の質問で
> 自発起動不発 → 事後 converge・延長 4 周で収束)の帰結。裁定が gate ① を兼ねる。

## 担当設備(equipment)

- 製造: requested/resolved `claude-fable-5-1`・Claude Code(Claude Agent SDK)・来歴 **self-reported**

## 0. 実測(起票根拠)

- **C16 は ECO-049 前の C17 と同じ穴**(実測 6 変種): フェンス内の様式例・チルダ fence・平文の言及
  (「収束 receipt は今回省略した」)・否定文・中身空の見出し・**判定なし receipt** がすべて通過。
  ECO-036 が要求する冒頭判定(収束/未収束)と起動経路は検査されていない。**未閉鎖 fence を除去
  しない**(C17 の ECO-051 より弱い正規表現)。免除宣言に行頭要求がなく、インデントされたコード例内の
  宣言が有効免除になる(C17 F11 と同型)。
- **偽陽性 1(潜在)**: ECO-036 様式どおり「round 軌跡: 2→0→0」とだけ書いた正当な receipt は
  `round\s*\d` に当たらず遮断される(現行 receipt は「round 1 = N 件」を併記するため未発現)。
- C17 は ECO-051 で修理済みだが、**C16 は C17 の雛形であり修理が親へ還流していない**(正本 2 つ)。
- **converge の収束判定の偽陰性 2 件**(このセッション): ECO-049(``` を見つけて ~~~ を探さず —
  表 A **型②** 最初のヒットで打ち切り)/ ECO-052(receipt 要件化で限界④が失効したのに古い前提で
  進行 — 表 A **型④** 開いているゲートの前提を再評価しない)。いずれも既知型の実施漏れ。
- **延長 round の再分類**: 「フェンス内の裁定語だけで required になる(偽 required)」は C16 の
  実装コメントに**意図された非対称**(過剰検出は出口があるので安全側・過剰免除は fail-open)と
  明記されており、実文を読まずに判定した**型③**(設計意図と実文の混同)の誤り → 宣言済み境界へ
  再分類し、required 判定は**変更しない**。
- 私の receipt は手順 6 の **DoD ✔/✘ を一度も書いていない**(ECO-051/052 order 実文)。
- 本 ECO の提案自体が converge の自発起動不発(人間の質問で発見)— 非起動系列の追加実測。

## 1. 変更要求(製造対象)

**a′(self-conformance.py)**
1. **receipt 検出ロジックの共通化**: `_strip_fences_all`(``` と ~~~・未閉鎖は EOF まで)と
   `_receipt_bodies(unfenced, heading_re)` を共通関数にし、C17(ECO-051 実装)と C16 の両方が使う。
   C17 は**挙動不変**(fixture F1〜F18+実 order 11 件で回帰確認)。
2. **C16 の receipt 判定**: fence 除去後に見出し検出(`# 2〜6+空白・receipt 語境界`)。
   **ECO-053 以降の order**(`C16_BODY_MIN = 53`)は本体ラベル 5 つを要求 — 判定(収束|未収束)・
   起動経路・round/周回・未収束事項・**DoD(✔|✘)**。ECO-033〜052 の order と improvements.md の
   節は**遺産規則**= fence 除去後のトークン共起(現行規則からフェンス穴だけ塞ぐ — 歴史は書き換えない)。
3. **`round 軌跡` 様式を受理**(`周回|round\s*(\d|軌跡)`)— 偽陽性の除去。
4. **免除宣言は行頭でのみ有効**(`^<!--`・re.M)。required 判定(hard-positive)は**変更しない**
   (意図された非対称 — §0)。
5. **C16 用 fixture を追加**(F10〜: フェンス内様式例 / チルダ / 未閉鎖 / 平文言及 / 中身空見出し /
   判定なし〔≥053〕/ インデント免除 / `round 軌跡` 様式の受理〔known-good〕/ 5 ラベル完備〔known-good〕)。
   ラベルの根拠= 本 ECO §0 の実測(C17 の独立検査官変種と同型)。
6. 限界宣言を改訂(共通化・遺産規則・ラベルの存在≠実施の真正・required の非対称)。

**d(converge.md 正本+写し)**: 手順 6 の判断待ち停止の文に「**先行する包括承認(「進めて」等)は
未収束の裁定点を代替しない** — 未収束はその時点で個別に裁定を得る」を 1 文追加(D-1 同期)。

**b′(improvements.md)**: 見逃し 2 件を型②・型④の**実施漏れ**として記帳(表 B 追記なし)+
本提案の converge 自発起動不発を非起動系列へ記帳。

**採らない**: required 判定の fence 除去(宣言済み非対称)/ 表 B への新型追記(反証済み)/
receipt 本体の意味検査 / 遺産 19 件+節への本体ラベル遡及。

## 2. 影響なし予測(反証可能・製造前に凍結)

diff は self-conformance.py(C16/C17 ブロック)・converge 正本+写し・improvements.md+台帳系。
C17: fixture 18/18・実 order 11 件の判定不変(共通化は挙動保存)。C16: 遺産規則で対象 19 order+
節は全通過を維持(製造前に fence 除去後トークン共起で実測済み — §4 に記載)。本 order(053)は
新規則の最初の適用個体で receipt に 5 ラベルを備える。C1/C13 判定不変。

## 3. 受入

- **V1**: C16 変種(§0 の 6+未閉鎖+インデント免除)が遮断へ反転・`round 軌跡` 様式が通過・
  5 ラベル known-good が通過・遺産 19 件+節が通過・C17 の F1〜F18 と 11 件が不変。
- **V2**: C16 fixture 全成立(既存 9+追加)・C17 fixture 18/18。
- **V3**: converge 正本と写しの差分が既知 2 点のみ・追加 1 文のみ(削除された文言なし)。
- **V4**: self-conformance 全 PASS。**V5**: CI 緑。**V6**: diff 窓。

## /preflight receipt(起動経路: 自発 — 既裁定の適用実装)

- 分類= 既裁定の適用実装(厳しい側= continuation)。状態: baseline `4703606`= **confirmed**/
  次番 053= **confirmed**/ C16 ブロック全文= **confirmed**(実読 L1021〜1229)/ C17 の共通化候補
  (`_C17_FENCE_RE`・`_c17_receipt_bodies`)= **confirmed**/ 延長 round の所見のうち「偽 required」=
  **contradicted → 再分類**(実装コメントの実文)/ 遺産対象の通過= **confirmed**(§4 実測)。
- 開始判定: **PROCEED**(contradicted 1 件は設計へ反映済み)・override 0。

## /converge receipt(起動経路: 人間の質問による事後適用 → 延長は人間裁定)

- **判定: 収束**(round 軌跡: 3→1→0 →〔延長〕2→1→0→0)。提示済み(前ターン)。本 order 執筆時の
  追加照合 1 件: 延長 round の「偽 required」を型③として再分類(§0)— 収束判定後の指摘として記録し
  receipt は書き換えない。
- **DoD(統治規則向け)**: アンカー ✔(verified 昇格・order 存在 / 未収束宣言)/ 実装先 ✔
  (converge_classify・converge_verdict・_c17_receipt_bodies / converge.md 手順 6)/ 規則固有の
  赤 fixture ✔(a′)・✘(d は散文 — 宣言)/ 所有者 ✔(self-conformance)・✘(d — 散文順守は非保証)。
- 検証した主張: C16 変種 6+未閉鎖+インデント免除の通過 / `round 軌跡` の遮断 / DoD 不記載 /
  見逃し 2 件の型分類 / hard-positive 非対称の実装コメント。
- 未収束事項: なし。

## 4. 製造と受入の実測(2026-09-02)

- diff 監査の窓: baseline `4703606`(起票直前 HEAD・起票+製造は同一窓)→ head は受入時に確定。
- a′ を適用(共通関数 `_strip_fences_all` / `_receipt_bodies`・C16 見出し+5 ラベル〔≥053〕・遺産は
  fence 除去後トークン共起・`round 軌跡` 受理・免除は行頭・fixture F10〜F17・限界 (4)(5))。C17 は
  `_C17_FENCE_RE`/`_c17_receipt_bodies` を共通関数へ委譲(挙動保存)。d・b′ を適用。
- **製造中の自己捕捉(偽陽性)**: 新規則が **ECO-053 自身の order を遮断** — 「起動経路」を receipt の
  見出し行(`## /converge receipt(起動経路: …)`= ECO-036 様式)に書いており本体になかった。見出し行は
  receipt の一部なのでラベル検索に含める形へ是正(`include_heading=True`・C16 のみ)。F16 型の
  偽陽性を push 前に捕捉した実例。
- **製造中の手順逸脱(工具)**: 本ツールの bash heredoc がバックスラッシュを解釈し、パッチ内の
  `"\n"` が実改行になって構文エラー 1 回(同一弧内で修復・構文確認済み)。同日の heredoc 失敗
  2 回も同因。**以後バックスラッシュ・三連バッククォートを含むパッチは Write でファイル化して実行**。
- **V1 実測**: C16 変種 8 本(フェンス内様式例・チルダ・未閉鎖・平文言及・否定文・中身空見出し・
  判定/DoD なし・インデント免除)= **8/8 遮断** / known-good 3 本(`round 軌跡` のみ〔遺産〕・5 ラベル
  完備・行頭免除)= **通過** / cutoff 以降の order **21/21 通過**(遺産 20 は自番号の遺産規則・053 は
  新規則)/ C17 fixture **18/18**・C17 実 order verified ≥041 **全通過**(挙動保存)。**V1 PASS**。
- **V2 実測**: C16 fixture **17/17**(既存 9+追加 8)成立。**V2 PASS**。
- **V3 実測**: converge 正本+写しは各 +3/−1(手順 6 の該当行 1 行を書き換えて 1 文を追記 — 削除された
  文言はなく既存文は新行に保持)・差分ブロックは既知 2 点。§3 の「削除 0」は行単位では −1 と読める
  ため「削除された文言なし」へ精密化。**V3 PASS**。
- V4 以降は §5 で確定。

### 較正 receipt(/calibrate 自己適用 — trigger ③: 検査器の変更直後。二軸)

- 査定した主張と判定:
  1. 「C16 の変種を遮断し、遺産と C17 を壊さない」— **observed / 適格**(8/8 反転・遺産 21/21・
     C17 18/18+実 order 全通過・fixture へ恒久化)。
  2. 「共通化は C17 の挙動を保存する」— **observed / 適格**(F1〜F18 と実 order 11 件が回帰網として
     機能 — ECO-051 で fixture を恒久化した実利)。
  3. 「本修理は当方設計の変種以外にも効く」— **unknown(理由コード: 未実行)** — C16 を検体とする
     盲検試験まで主張しない(ECO-051 と同じ留保)。
- 検出した計器欠陥: 製造中に新規則の**偽陽性 1 件**(見出し内ラベルの不認識)を自己捕捉し是正。
- 検出力の限界: fixture は当方が C17 の独立検査官変種から**転記した**もの — C16 固有の未知変種は
  未測定。required 判定の fence 内読取は意図された非対照であり本 ECO は測っていない。
- battery 行別記録:

  | Q | asked/NA | 判定 | 実測 or 読解 | 所見 |
  |---|---|---|---|---|
  | Q1 | asked | observed/適格 | 実測 | 限界宣言とコードの一致(遺産規則・ラベル≠真正) |
  | Q2 | asked | observed/適格 | 実測 | known-bad/known-good 対(ラベル= 本 ECO 実測・C17 変種の転記) |
  | Q3 | asked | observed/適格 | 実測 | 各変種が独立に落ちる(fixture 個別) |
  | Q4 | asked | observed/適格 | 実測 | fixture ablation で較正不成立へ |
  | Q5 | asked | observed/適格 | 実測 | fixture 0 本・台帳欠測= FAIL(既存) |
  | Q6 | asked | observed/適格 | 実測 | check() 経由 |
  | Q7 | asked | observed/適格 | 実測 | fixture が陽性対照 |
  | Q8 | asked | observed/適格 | 実測 | 行頭の根拠つき免除は通過・件数表示(既存) |
  | Q9 | NA | — | — | 個体刻印は env_imprint の責務 |
  | Q10 | asked | observed/適格 | 読解 | 限界(1)〜(4) |
  | Q11 | asked | observed/適格 | 実測 | fence 2 種・未閉鎖・見出し変種・判定有無をクラス別に |

## 5. CI 実測(V5)

- (push 後に記入)

## 6. クローズ

- (受入時に記入)
