C1: OK — YAML ファイル群が 0 件なら `bool(files)` が偽になり FAIL する。

C2: OK — JSON ファイル群が 0 件なら `bool(files)` が偽になり FAIL する。

C3: SILENT — `verified` entry 群が 0 件でも `not problems` で PASS し、メッセージは「全 verified 適合」として件数を表示しない。

C4: OK — 固定的な scaffold 煙試験であり、生成 YAML 件数が 0 件でも `AGENTS.md` 等の固定検査が成立条件になる。

C5a: OK — 固定された不存在リポジトリへの fail-closed 操作であり、走査母集団を持たない。

C5b: OK — 固定された不存在リポジトリへの fail-closed 操作であり、走査母集団を持たない。

C6a: OK — 固定 fixture に対する rejected 判定操作であり、走査母集団を持たない。

C6b: OK — 固定 fixture に対する rejected 判定操作であり、走査母集団を持たない。

C7: OK — README の claims が 0 件でもメッセージに `[]` と `SKILLS` 実数が表示される。

C8: SILENT — `ROOT.iterdir()` の直下エントリが 0 件でも `fossils` は空で PASS し、「化石なし」とだけ表示する。

C9: OK — `suites` が 0 件なら明示的に FAIL し、各 suite の実測結果が 0 件でも `_c9_suite_verdict` が FAIL にする。

C10: OK — 固定された正本・派生スキーマ・テンプレートの突合であり、空の走査母集団に依存しない。

C11: OK — 合成 scaffold 上の固定的な工程設備・変異対照を実行するゲートであり、走査母集団を持たない。

C11b: OK — 固定された非既定 profile の対照を実行するゲートであり、走査母集団を持たない。

C12: OK — 相対リンクが 0 件でも PASS メッセージに `0 件` と表示される。

C13: OK — リポ文脈・設置先文脈とも `files` と `links` の 0 件を `bool(...)` / `total > 0` で FAIL にする。

C14: OK — 固定された FRESH/STALE/UNKNOWN/TAMPERED/EXTRA/INPUT/REAL の対照を実測するゲートであり、走査母集団を持たない。

C15: OK — Markdown corpus が 0 件なら明示的に FAIL し、deprecated 宣言が 0 件でもその旨をメッセージに表示する。

C16: OK — 対象 artifact と improvements 節が 0 件でもメッセージに `以降 0 件` と表示される。

C17: OK — fixture が 0 件なら FAIL し、適用対象 `verified` entry が 0 件でも明示的に FAIL する。

C18: OK — hook と `hooksPath` の設置状態を検査する固定操作であり、走査母集団を持たない。