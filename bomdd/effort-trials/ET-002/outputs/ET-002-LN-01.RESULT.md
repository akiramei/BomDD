C1: OK — YAML ファイル群が 0 件なら `bool(files)` が偽で FAIL。

C2: OK — JSON ファイル群が 0 件なら `bool(files)` が偽で FAIL。

C3: SILENT — `verified` entry 群が 0 件でも `problems=[]` のまま PASSし、件数を表示しない。

C4: OK — 固定の scaffold 操作を実行し、生成物・lock・manifest・AGENTS 等の結果を判定する。

C5a: OK — 固定の不存在リポに対する陽性対照で、exit 2 と測定不能マーカーを要求する。

C5b: OK — 固定の不存在リポに対する陽性対照で、exit 2 と測定不能マーカーを要求する。

C6a: OK — 固定 fixture の rejected 規則を検査する操作で、走査母集団に依存しない。

C6b: OK — 固定 fixture の rejected 規則を検査する操作で、走査母集団に依存しない。

C7: SILENT — README のスキル本数 claims が 0 件でも `all(...)` が真になり、件数表示は `[]` と実数だけで母集団 0 を示さない。

C8: SILENT — リポ直下 entry が 0 件でも `fossils=[]` により PASSし、件数を表示しない。

C9: OK — suites が空なら即 FAILし、各 suite でも結果 0 件または total 0 は FAIL。

C10: OK — 固定の正本・派生スキーマ・テンプレートを読み込み、欠測や乖離を FAIL にする。

C11: OK — 固定の git、scaffold、runner、IQ/OQ、変異対照を実行する。

C11b: OK — 固定の非既定 profile を生成し、全対照の結果を判定する。

C12: SILENT — AGENTS.md の相対リンク群が 0 件でも `not missing` が真で PASSし、メッセージはリンク数 0 を表示しない。

C13: OK — repo files、リンク数、設置先 files、リンク数をそれぞれ `>0` 要件で検査する。

C14: OK — 固定の合成 origin/kit と実 scaffold を用いた 7 分岐の対照実測を要求する。

C15: OK — md corpus が 0 件なら即 FAILし、deprecated 宣言 0 件でも明示的に表示する。

C16: SILENT — cutoff 以降の対象 targets と improvements 節がともに 0 件でも `not problems` で PASSし、件数表示は対象総数 0 を母集団件数として明示しない。

C17: OK — fixture 0 本は FAIL、適用対象 verified 0 件も `problems` に追加して FAIL。

C18: OK — 固定の git hooksPath と pre-push hook の設置状態を検査し、CI では NA を明示する。