C1: OK — YAML ファイル群は 0 件なら `bool(files)` により FAIL、PASS 時は件数表示。

C2: OK — JSON ファイル群は 0 件なら `bool(files)` により FAIL、PASS 時は件数表示。

C3: SILENT — `verified` エントリ群が空でも `not problems` で PASS し、メッセージは「全 verified 適合」で件数表示なし。

C4: OK — 固定の scaffold 操作を検査するゲートで、走査母集団依存の空集合 PASS ではない。

C5a: OK — 固定の欠落リポジトリに対する fail-closed 対照操作で、走査母集団を持たない。

C5b: OK — 固定の欠落リポジトリに対する fail-closed 対照操作で、走査母集団を持たない。

C6a: OK — 固定 fixture を必ず生成し、理由なし `rejected` の exit 非 0 を検査する。

C6b: OK — 固定 fixture を必ず生成し、根拠つき `rejected` の exit 0 を検査する。

C7: SILENT — README のスキル本数 claims が空でも `all([])` が真で、件数なしの `claims = []` を表示して PASS。

C8: SILENT — 直下エントリ群が空でも `not fossils` で PASSし、「化石なし」とだけ表示。

C9: OK — manifest の suites が空なら明示的に FAIL。各 suite も結果 0 件または total 0 なら FAIL。

C10: OK — 固定の正本・派生スキーマ・テンプレートを読むゲートで、空の対象集合を無音 PASS する走査判定ではない。

C11: OK — 固定 scaffold 上で git、runner、IQ/OQ、変異対照を実行するゲートで、空母集団依存ではない。

C11b: OK — 固定の非既定 profile scaffold と全対照を実行するゲートで、空母集団依存ではない。

C12: SILENT — AGENTS.md の相対 Markdown リンク群が 0 件でも `not missing` で PASSし、メッセージは「0 件すべて実在」と件数表示なし。

C13: OK — リポ文脈・設置先文脈ともファイルとリンクが 0 件なら `total_a > 0` または `total_b > 0` により FAIL。

C14: OK — 固定の合成 origin/kit と実 scaffold を作り、7 分岐の結果件数を表示して判定。

C15: OK — Markdown corpus が 0 件なら明示的に FAIL。deprecated 宣言 0 件でも corpus は存在し、明示記録つきで PASS。

C16: SILENT — cutoff 後の order と improvements 節がともに 0 件でも `not problems` で PASSし、メッセージの件数は `0 件` と表示されるため定義上は OK。