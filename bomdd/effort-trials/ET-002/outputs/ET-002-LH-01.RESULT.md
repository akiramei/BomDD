C1: OK — YAML 母集団が 0 件なら `bool(files)` により FAIL する。  
C2: OK — JSON 母集団が 0 件なら `bool(files)` により FAIL する。  
C3: SILENT — `verified` エントリが 0 件でも `not problems` で PASS し、件数を表示しない。  
C4: SILENT — 生成 YAML または manifest の `files` が 0 件でも空走査・件数不表示のまま他条件だけで PASS できる。  
C5a: OK — 固定の不存在リポジトリに対する fail-closed 検査で、対象欠落時は exit 2 を要求する。  
C5b: OK — 固定の不存在リポジトリに対する fail-closed 検査で、対象欠落時は exit 2 を要求する。  
C6a: OK — 固定 fixture の理由なし `rejected` を実行し、exit 非 0 を要求する。  
C6b: OK — 固定 fixture の根拠つき `rejected` を実行し、exit 0 を要求する。  
C7: OK — README の claims が 0 件でも `[]` をメッセージに表示する。  
C8: SILENT — リポ直下の化石候補が 0 件でも「なし」とだけ表示して PASS する。  
C9: OK — suites、test project、実測 results の空集合を各ガードで FAIL にする。  
C10: SILENT — UI IR／trace-map テンプレの検査キーが 0 件でも空振り検査が PASS し、キー数を表示しない。  
C11: SILENT — 初回 qualification の IQ/OQ 結果が空でも returncode・集約 disposition・決定性だけで PASS でき、結果件数を表示しない。  
C11b: SILENT — `iq` と `oq` が空でも `failed=[]` となり、決定性と `not failed` だけで PASS できる。  
C12: OK — 相対リンクが 0 件でもメッセージに `0 件` を表示する。  
C13: OK — リポ／設置先のファイル・リンク母集団が 0 件なら `bool`／`total > 0` で FAIL する。  
C14: OK — 固定の FRESH/STALE/UNKNOWN/TAMPERED/EXTRA/INPUT/REAL 対照を実測し、結果件数を表示する。  
C15: OK — deprecated 宣言が 0 件でも「宣言 0 件」と表示し、MD corpus が 0 件なら FAIL する。  
C16: OK — 対象が 0 件でも判定メッセージに `0 件` を表示する。  
C17: OK — 適用対象の verified が 0 件なら明示的に FAIL する。  
C18: OK — 固定の hooksPath／hook 設置検査であり、CI では NA を明示して PASS する。