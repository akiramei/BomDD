C1: OK — YAML ファイル群が 0 件なら `bool(files)` が偽で FAILし、メッセージにも `YAML 0 件` と表示される。
C2: OK — JSON ファイル群が 0 件なら `bool(files)` が偽で FAILし、メッセージにも `JSON 0 件` と表示される。
C3: SILENT — `verified` entry が 0 件でも `problems` は空のまま PASSし、メッセージは「全 verified 適合」で件数を表示しない。
C4: SILENT — 生成 YAML 群が 0 件でも `gen_bad` が空なので通過でき、「生成 YAML 厳格パース 全数」とだけ表示されて件数が出ない（空の manifest `files` も `0 == 0` で通過し件数非表示）。
C5a: OK — 存在しないリポに対する単一の固定異常系操作であり、走査母集団を持たない。
C5b: OK — 存在しないリポに対する単一の固定異常系操作であり、走査母集団を持たない。
C6a: OK — コード内で必ず生成される固定 fixture に対する単一の変異試験であり、空になり得る走査母集団を持たない。
C6b: OK — コード内で必ず生成される固定 fixture に対する単一の正常系試験であり、空になり得る走査母集団を持たない。
C7: OK — README の本数表記群が 0 件でも PASS 行に `claims=[]` が表示され、SKILLS 数も数字で表示される。
C8: SILENT — `ROOT.iterdir()` の直下 entry 群が 0 件なら `fossils=[]` により PASSし、メッセージには走査した entry 数も空リストも出ない。
C9: OK — suites 0 件は明示 FAIL、project 母集団の空・不一致も双方向突合で FAILし、各 suite の results 0 件または `total=0` も FAILする。
C10: SILENT — `ui-ir.json` や `ui-trace-map.json` のテンプレートキー群が空でも空振りリストは空のまま通過でき、PASS メッセージにキー件数や空リストが出ない。
C11: SILENT — 通常 qualification 結果の `iq`/`oq` control 群が 0 件でも、return code・`disposition`・`runs_identical` が適合すれば通過でき、その control 件数を表示しない。
C11b: SILENT — 非既定構成結果の `iq`/`oq` control 群が 0 件なら `failed=[]` となり、return code と集約フラグ次第で PASSし、control 件数や空リストを表示しない。
C12: OK — 相対リンク群が 0 件でも PASS 行に `相対リンク 0 件` と明示される。
C13: OK — リポ文脈・設置先文脈の file 群は `bool(...)`、link 群は `total > 0` でそれぞれ空集合を FAILし、両方の件数も表示する。
C14: OK — FRESH 等の固定された7対照を必ず `results` に追加する固定操作ゲートで、結果も `x/7` と表示される。
C15: OK — Markdown corpus 0 件は明示 FAILし、任意の deprecated 宣言群が 0 件の場合も「deprecated 宣言 0 件」と表示する。
C16: OK — 実対象は cutoff 以降の order と improvements 節を結合した集合として `n` 件を表示し、fixture 群も `件数/件数`、免除群も `0 件` を含めて表示する。
C17: OK — fixture 0 本は明示 FAILし、適用対象の verified ECO が 0 件でも `n == 0` により FAILするうえ、対象・fixture・免除の各件数を表示する。
C18: OK — hooksPath と単一 hook の設置状態を確認する固定操作であり、走査母集団を持たない。