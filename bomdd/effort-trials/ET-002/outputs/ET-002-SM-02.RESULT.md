C1: OK — YAML ファイル群が 0 件なら `bool(files)` が偽となり FAILし、メッセージにも `YAML 0 件` が出る。
C2: OK — JSON ファイル群が 0 件なら `bool(files)` が偽となり FAILし、メッセージにも `JSON 0 件` が出る。
C3: SILENT — `changes` または verified entry が 0 件でも `problems=[]` のまま PASSし、メッセージは「全 verified 適合」で件数を示さない。
C4: SILENT — 生成 YAML 群や絶対パス漏れ走査対象群、manifest の files 群には非空ガードがなく、0 件でも PASS可能で走査母集団の件数を表示しない。
C5a: OK — 存在しないリポに対する単一の固定異常系操作であり、列挙される走査母集団を持たない。
C5b: OK — 存在しないリポに対する単一の固定異常系操作であり、列挙される走査母集団を持たない。
C6a: OK — 固定 fixture に対する「理由なし rejected」の単一変異操作であり、可変の走査母集団を持たない。
C6b: OK — 固定 fixture に対する「根拠つき rejected」の単一正常系操作であり、可変の走査母集団を持たない。
C7: OK — README の本数表記群が空ならメッセージに `[]`、SKILLS が空なら実数 `0` が表示される。
C8: SILENT — `ROOT.iterdir()` の走査対象が 0 件でも `fossils=[]` により PASSし、空集合も走査件数もメッセージに出ない。
C9: OK — suites は空なら FAILし、csproj と manifest project は双方の件数を表示し、各 suite の results または total が 0 なら FAILする。
C10: SILENT — ui-ir／ui-trace-map テンプレの実在キー群が 0 件でも空振り検査は問題を追加せず、PASSメッセージにテンプレキー件数を示さない。
C11: OK — 明示的に走査する変異後 IQ 群が空なら `bool(iq3)` が偽となって FAILし、ほかは固定の scaffold・qualification 操作である。
C11b: SILENT — qualification JSON の `iq`＋`oq` control 群が 0 件でも `failed=[]` となり、外側の終了値と disposition が PASSなら通過でき、control 件数を表示しない。
C12: OK — 相対リンク群が 0 件でも PASSするが、メッセージに `相対リンク 0 件` と表示される。
C13: OK — リポ／設置先それぞれでファイル群の非空とリンク数 `> 0` を要求し、両方の files・links 件数も表示する。
C14: OK — FRESH 等の7分岐は固定操作であり、結果は成功数／全数として `x/7` 形式で表示される。
C15: OK — md corpus が 0 件なら明示的に FAILし、deprecated 宣言が 0 件なら「宣言 0 件」と表示される。
C16: SILENT — order targets と improvements sections は別母集団だが非空ガードがなく、一方だけ 0 件でも合算 `n` が非ゼロなら PASSし、どちらが空かを表示しない。
C17: OK — fixture 群が 0 本なら FAILし、適用対象の verified entry が 0 件でも `problems` に追加して FAILする。
C18: OK — hooksPath と pre-push hook の設置確認という固定操作であり、列挙される走査母集団を持たない。