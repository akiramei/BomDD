C1: OK — YAML 母集団が 0 件なら `bool(files)` により FAIL になる。  
C2: OK — JSON 母集団が 0 件なら `bool(files)` により FAIL になる。  
C3: SILENT — `verified` entry が 0 件でも `not problems` で PASS し、件数を表示しない。  
C4: SILENT — 生成 YAML または manifest の kit files が 0 件でも非空ガードがなく、件数を表示しない。  
C5a: OK — 固定の不存在リポジトリ操作であり、`exit 2` と測定不能マーカーを要求する。  
C5b: OK — 固定の不存在リポジトリ操作であり、`exit 2` と測定不能マーカーを要求する。  
C6a: OK — 固定 fixture による rejected 判定操作で、空母集団走査ではない。  
C6b: OK — 固定 fixture による rejected 判定操作で、空母集団走査ではない。  
C7: OK — claims が空でも PASS 行に `[]` と実数が表示される。  
C8: SILENT — 化石集合が 0 件でも `not fossils` で PASS し、件数や空リストを表示しない。  
C9: OK — suites 空は FAIL、対象 project の双方向突合と各 suite の空結果・total 0 も FAIL になる。  
C10: SILENT — template の検査対象キーが 0 件でも空振り検査を通過し、キー件数を表示しない。  
C11: SILENT — qualification 結果の IQ/OQ 集合が空でも disposition・決定性だけで PASS し、件数を表示しない。  
C11b: SILENT — IQ/OQ 結果が空でも `failed=[]` と決定性だけで PASS し、結果件数を表示しない。  
C12: OK — 相対リンクが 0 件でも PASS 行に `0 件` と表示される。  
C13: OK — リポ文脈・設置先文脈ともファイルまたはリンクが空なら明示ガードで FAIL になる。  
C14: OK — 固定された全分岐結果を常に append し、PASS 行に実測件数/総数を表示する。  
C15: OK — md corpus が空なら FAIL、deprecated 宣言が 0 件でも PASS 行に `0 件` と表示される。  
C16: SILENT — register targets または improvements sections の一方が 0 件でも個別ガードがなく、合算件数だけを表示する。  
C17: OK — fixture 0 本および適用対象 verified 0 件を明示的に FAIL にする。  
C18: OK — 固定の hooksPath・hook 実在確認（CI は明示的 NA PASS）で、走査母集団を持たない。