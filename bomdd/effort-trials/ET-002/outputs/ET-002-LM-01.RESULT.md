C1: OK — YAML 母集団が 0 件なら `bool(files)` により FAIL になる。  
C2: OK — JSON 母集団が 0 件なら `bool(files)` により FAIL になる。  
C3: SILENT — `verified` entry 群が 0 件でも `problems` が空なら PASS し、件数を表示しない。  
C4: OK — scaffold 固定操作と lock/manifest・AGENTS・生成 YAML の検査を行う。  
C5a: OK — 欠落リポジトリに対する固定の fail-closed 陽性対照。  
C5b: OK — 欠落リポジトリに対する固定の fail-closed 陽性対照。  
C6a: OK — 合成 rulings fixture に対する固定の拒否判定対照。  
C6b: OK — 合成 rulings fixture に対する固定の受理判定対照。  
C7: OK — README の claims が空でも PASS 行に `[]` と SKILLS 実数を表示する。  
C8: SILENT — 化石ファイル母集団が 0 件でも PASS し、空件数・空リストを表示しない。  
C9: OK — manifest の suites が 0 件なら明示的に FAIL する。  
C10: OK — 正本・派生スキーマ等の固定必須構造の欠落は drift として FAIL になる。  
C11: OK — scaffold 上で git/IQ/OQ/変異対照を実行する固定操作。  
C11b: OK — 非既定 profile を生成して全対照を実行する固定操作。  
C12: OK — Markdown リンクが 0 件でも PASS 行にリンク件数 `0` を表示する。  
C13: OK — リポ文脈または設置先文脈のファイル・リンク母集団が空なら FAIL になる。  
C14: OK — 固定 fixture 群を実測し、PASS 行に実測件数を表示する。  
C15: OK — Markdown corpus が 0 件なら FAIL、deprecated 宣言が 0 件でも PASS 行に `0` を表示する。  
C16: OK — 対象が 0 件でも PASS 行に対象件数 `0` を表示する。  
C17: OK — 適用対象の verified entry が 0 件なら明示的に FAIL する。  
C18: OK — hooksPath と hook 実在を確認する固定の設置検収操作。