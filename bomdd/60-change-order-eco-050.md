# Change Order — ECO-050(calibrate 自身の検出力限界宣言 — 第 6 掃引からの確定・案 B)

> 裁定: user 2026-09-02「案 B も採択して製造まで進めて」— calibrate 自己較正(第 6 掃引)の
> 起票範囲裁定の第 2 弾。案 A は ECO-049 で実施済み。案 C(問うた battery 行の receipt 記録)は
> 見送り継続。裁定が gate ① を兼ねる。

## 担当設備(equipment)

- 製造: requested/resolved `claude-fable-5`・Claude Code(Claude Agent SDK)・来歴 **self-reported**

## 0. 実測(起票根拠 — 第 6 掃引の declaration 所見(a))

`calibrate.md` に**自身の「検出力の限界」節がない** — Q10 で他計器に課している要求を自分は
満たしていない。実測(grep): calibrate.md の「検出力の限界」出現 2 箇所は **Q10 の問い**と
**receipt の記載要件**であり、いずれも**他者への要求**。対照= preflight.md・worklist.py・
ui-cad-gate.py・process-validator.py はいずれも**自前の限界節**を持つ。

## 1. 変更要求(製造対象 — 案 B のみ)

`calibrate.md` **正本**へ「## 検出力の限界(宣言)」節を追加し、**写し**(`.claude/skills/`)へ
同期する(D-1 同期規則 — プレースホルダーは自リポ相対へ解決)。配置は preflight.md と同じく
**「学習ループ(停止点)」の直前**(様式一致)。内容は**第 6 掃引の実測からの転記のみ・
新規限界の創作なし**(ECO-045/047/049 と同じ「掃引後確定」の運び)。

**battery(Q1〜Q11)・手順・判定様式・receipt 要件は一切変更しない** — 凍結の実質
(測定条件= battery 不変)は維持される。本 ECO が解凍するのは「本文に節を足さない」という
運用上の停止だけであり、測定条件そのものではない。

**採らない**: 案 C(問うた battery 行の receipt 記録 — 記録コスト増・実害未実測)/
手順 4 の判定様式の変更(「所見なし」の意味論は限界節で述べるに留め、判定語彙は不変)/
リポ内リンクの追加(既存様式どおり ECO 番号はテキスト参照 — C13 二文脈検査への影響を作らない)。

## 2. 影響なし予測(反証可能・製造前に凍結)

diff は calibrate.md 正本+写し+台帳系。**節の追加のみ(既存行の削除・改変なし)**。
C13(リンク実在の二文脈検査)は新規リンクを作らないため判定不変。正本と写しの差分は
既知の 2 点(写し header・表 A 注記の言い換え)のまま。self-conformance 全検査の判定不変。
C16/C17 は本 order を required と判定する見込み — receipt 埋め込み済み。

## 3. 受入

- **V1**: 追加した全項が第 6 掃引の実測へトレース可能(新規限界の創作なし)。かつ
  battery・手順・判定様式・receipt 要件に**差分がない**こと(diff で確認)。
- **V2**: 正本と写しの差分が**既知の 2 点のみ**であること(同期成立の機械確認)。
- **V3**: self-conformance 全 PASS。**V4**: CI 緑(headSha 照合)。**V5**: diff 窓。

## /preflight receipt(起動経路: 自発 — 既裁定の適用実装)

- 分類= 既裁定の適用実装(契約 2 クラス外につき厳しい側= continuation を適用)。
- 状態: baseline `eb13bf3`= **confirmed**(HEAD 実測・作業木クリーン)/ 次番 050=
  **confirmed**(register 末尾= 049)/ 正本と写しの同期実態= **confirmed**(diff 実測で
  既知 2 点のみ)/ 第 6 掃引の所見= **confirmed**(前掃引で実測・ECO-049 order に記録)/
  preflight.md の限界節配置= **confirmed**(実読)。
- 開始判定: **PROCEED**(stale/contradicted/unknown なし)・override 0。

## /converge receipt(起動経路: 自発〔統治文書= skill 正本の設計合成〕)

- **判定: 収束**(round 軌跡: 4→0→0)。round 1 = 4 件 — ①凍結方針との整合を order に明記
  (battery 不変=測定条件は維持・解凍するのは運用上の停止のみ)②起動の機械化に関する項は
  C17 docstring と二重正本化しうる → **正本は C17 側**とし skill 側は読み手向けの要約に留める
  ③手順 4 の判定語彙変更は案 B の範囲外 → 限界節で述べるに留める(不採用)④リポ内リンクを
  足すと C13 二文脈検査へ影響 → 既存様式どおり ECO 番号のテキスト参照にする。
- 検証した主張: calibrate.md の限界節不在(grep 実測・出現 2 箇所は他者への要求)/ 対照 4 計器が
  自前の限界節を持つこと(grep -c 実測)/ 正本と写しの差分(diff 実測)/ preflight.md の
  配置様式(実読)。
- 未収束事項: なし。

## 4. 製造と受入の実測(2026-09-02)

- diff 監査の窓: baseline `eb13bf3`(起票直前 HEAD・起票+製造は同一窓)→ head は受入時に確定。
- 「## 検出力の限界(宣言 — 本手順が測っていない次元)」節を**学習ループの直前**へ追加
  (preflight.md と同じ配置)。5 項目とも第 6 掃引の実測からの転記。
- **V1 実測**: 正本+写しで **44 行の挿入のみ・削除 0 行**(`git diff -U0 | grep '^-[^-]'` = 0)—
  battery(Q1〜Q11)・手順・判定様式・receipt 要件に差分なし。各項のトレース先= ①第 6 掃引の
  二軸判定(検出は査定者のプローブ設計に依存)②同 boundary(陽性対照の不在)+ ECO-049 の
  偽陰性実測 ③同 reference-truth deficiency ④同 declaration(b)+ Q4/Q5 未発火の grep 実測
  ⑤ C17 docstring 限界 (2)。**V1 PASS**。
- **V2 実測**: 正本と写しの差分は**既知 2 点のみ**(写し header ブロック / 表 A 注記の
  プレースホルダー解決)— 差分ブロック数 2。**V2 PASS**。
- V3 以降は §5 で確定。

### 較正 receipt(/calibrate 自己適用 — trigger ③: 検査器〔本スキル自身〕の変更直後。二軸)

- 査定した主張と判定:
  1. 「追加した 5 項は掃引実測の転記であり新規創作がない」— **observed / 適格**
     (各項のトレース先を §4 に明示・全項が縮小方向の宣言= Q1 の逆方向)。
  2. 「battery・手順・判定様式は不変(測定条件の凍結は維持)」— **observed / 適格**
     (削除 0 行・挿入のみを diff で実測)。
  3. 「正本と写しが分岐していない」— **observed / 適格**(差分ブロック 2・既知の 2 点のみ)。
  4. 「限界宣言が次の査定者の読み方を実際に変える(実効到達)」— **unknown(理由コード:
     未実行)** — ECO-047/048 と同種。
- 検出した計器欠陥: なし(本 ECO は第 6 掃引で検出済み declaration 所見の是正)。
- 検出力の限界: 本査定は**節追加の妥当性**のみ。宣言した 5 項目そのものの正しさ(たとえば
  「査定者依存」の程度)は測っておらず、**限界の宣言は限界の解消ではない**。

## 5. CI 実測(V4)

- (push 後に記入)

## 6. クローズ

- (受入時に記入)
