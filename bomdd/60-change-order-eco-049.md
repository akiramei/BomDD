# Change Order — ECO-049(C17 の use/mention 弁別 — 第 6 掃引〔calibrate 自己較正〕からの確定)

> 裁定: user 2026-09-01「案 A を採択して製造まで進めて」— calibrate 自身を題材とした
> 第 6 掃引の起票範囲裁定。案 B(calibrate.md への自前限界節= skill 本文の解凍)・案 C
> (問うた battery 行の receipt 記録)は見送り。裁定が gate ① を兼ねる。

## 担当設備(equipment)

- 製造: requested/resolved `claude-fable-5`・Claude Code(Claude Agent SDK)・来歴 **self-reported**

## 0. 実測(起票根拠 — 第 6 掃引の所見)

対照つきプローブで **C17 が use/mention を弁別しない**ことを実測(P1 フェンス内の例示= 通過 /
P2「較正 receipt は今回省略した」= 通過 / P3 否定文= 通過 / 対照 R1 receipt なし= 遮断・
R2 フェンス内免除= 遮断)。構造は**非対称** — 免除経路はフェンス防御つき(F5 が恒久較正)
なのに、receipt 経路は生テキストの部分文字列検索で文脈を見ない。**存在の否定文が存在の検査を
通過する**。型= calibrate 失敗型表 **⑧ use/mention の混同**(出典 ECO-033/034)。
**実害 0**(C17 適用中の 8 件は全て実見出しの receipt 節を持つ — 実測)。

本欠陥は **C17 を新設した ECO-043 の較正 receipt(trigger ①+③ 明記)で検出されていない** —
第 6 掃引で得た**偽陰性の実測 1 件**(「capability=0 は健全の証拠ではない」の実証)。

## 1. 変更要求(製造対象 — 案 A のみ)

`method/tools/self-conformance.py` の C17:

1. **receipt 検出をフェンス除去後に行う**(免除経路と同じ前処理へ揃える — 非対称の解消)。
2. **receipt を「見出し」として要求する** — `C17_RECEIPT_RE` を部分文字列から**見出し行**
   (`^ #{2,6} … 較正 receipt|calibrate receipt`)へ。宣言済み限界 (1) の「**構造的存在**」を
   厳密化するものであり、**中身の質は依然として測らない**(案 C を採らないため)。
3. **known-bad / known-good 腕を fixture へ追加**(Q2 — 修理には reference 較正を付ける):
   F7= フェンス内の見出しのみ→ FAIL / F8= 平文の言及のみ→ FAIL / F9= 見出しあり→ PASS。
4. **FAIL メッセージを行動可能に**(「言及・フェンス内例示は receipt ではない」を明示)。
5. **限界宣言 (1) を改訂**(見出しとしての構造的存在のみ・中身の質は非測定)+ 本 ECO 由来を刻む。

**採らない**: 中身の実質検査(案 C 領分)・trigger ②③④ の被覆(限界 (2) 維持)。

## 2. 影響なし予測(反証可能・製造前に凍結)

diff は self-conformance.py の C17 節+台帳系。**C17 適用 8 件は全て通過を維持**(製造前に
見出し正規表現で実測済み — 8/8 見出しあり)。既存 fixture F1〜F6 の期待値は不変
(F1 は見出し形式・F4〜F6 は免除経路で見出しを持たない)。他検査の判定は不変。
C16 は本 order を required と判定する見込み — receipt 埋め込み済み。

## 3. 受入

- **V1**: 掃引プローブの再実測で P1/P2/P3 が**遮断**へ反転し、P4(見出しのみ・中身空)は
  **通過を維持**(宣言済み境界の射程を広げていないこと= 過剰厳格化していないこと)。
  対照 R1/R2 は遮断のまま。
- **V2**: C17 適用 8 件が全て通過(件数・免除 0 が不変)・self-conformance 全 PASS。
- **V3**: CI 緑(headSha 照合)。**V4**: diff 窓。

## /preflight receipt(起動経路: 自発 — 既裁定の適用実装)

- 分類= 既裁定の適用実装(契約 2 クラス外につき厳しい側= continuation を適用)。
- 状態: baseline `5f2c7da`= **confirmed**(HEAD 実測・作業木クリーン)/ 次番 049=
  **confirmed**(register 末尾= 048)/ C17 実装座標・限界宣言= **confirmed**(実読)/
  プローブ結果と実害 0 = **confirmed**(第 6 掃引で実測)。
- 開始判定: **PROCEED**(stale/contradicted/unknown なし)・override 0。

## /converge receipt(起動経路: 自発〔検査器仕様の設計合成〕)

- **判定: 収束**(round 軌跡: 2→0→0)。round 1 = 2 件 — ①見出し要求は「中身も要求」へ
  滑りやすい(限界 (1) の射程を保つと明記し、案 C との境界を order へ固定)②修理だけで
  fixture を足さないと**同じ穴を二度目に検出できない**(known-bad 腕 F7/F8 を要件へ格上げ)。
- 検証した主張: 免除経路のフェンス防御は既存(F5 実文)/ 受入 8 件は見出し形式(実測)/
  宣言済み限界 (1) の文言(docstring 実文)。
- 未収束事項: なし。

## 4. 製造と受入の実測(2026-09-01)

- diff 監査の窓: baseline `5f2c7da`(起票直前 HEAD・起票+製造は同一窓)→ head は受入時に確定。
- 製造 5 点を実施(receipt 検出をフェンス除去後へ / 見出し要求へ / F7〜F9 追加 /
  FAIL メッセージ行動可能化 / 限界宣言 (1) 改訂)。
- **V1 実測(掃引プローブの再実測・対照つき)**: P1 フェンス内例示= **遮断へ反転** /
  P2 「receipt は省略した」= **遮断へ反転** / P3 否定文= **遮断へ反転** /
  P4 見出しのみ・中身空= **通過を維持**(宣言済み境界を広げていない= 過剰厳格化なし)/
  対照 R1(receipt なし)・R2(フェンス内免除)= 遮断のまま / R3(根拠つき免除)= 通過のまま。
  **7/7 期待一致 — V1 PASS**。
- **V2 実測**: C17 適用 **8 件・不通過 0 件**(件数不変)。fixture 較正 **9/9 成立**
  (F1〜F6 の期待値不変+新設 F7/F8= known-bad 赤・F9= known-good 緑)。
- V2 後半(全検査)・V3・V4 は §5 で確定。

### 較正 receipt(/calibrate 自己適用 — trigger ③: 検査器の変更直後。二軸)

- 査定した主張と判定:
  1. 「use/mention を弁別するようになった」— **observed / 適格**(対照つき 7 本の反転実測 —
     known-bad 2 本を fixture へ恒久化したので次回以降も再検出可能)。
  2. 「過剰厳格化していない(既存の正当な receipt を落とさない)」— **observed / 適格**
     (適用 8 件通過・P4 と R3 の維持が「射程を広げていない」ことの陽性対照)。
  3. 「見出し要求は将来の正当な receipt 様式を排除しない」— **unknown(理由コード: 未実行)**
     — 見出しを使わない receipt 様式が今後現れた場合の偽陽性は未観測(出口= 根拠つき免除
     が既にある)。
- 検出した計器欠陥: なし(本 ECO は第 6 掃引で検出済みの欠陥の修理)。
- 検出力の限界: 本修理は**見出しとしての存在**までを厳密化したにすぎず、**中身の実質**は
  依然として測らない(案 C 領分)。trigger ②③④ は被覆外のまま(限界 (2))。

## 5. CI 実測(V3)

- (push 後に記入)

## 6. クローズ

- (受入時に記入)
