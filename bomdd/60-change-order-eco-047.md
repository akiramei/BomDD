# Change Order — ECO-047(worklist.py の検出力限界宣言 — 第 5 掃引からの確定)

> 裁定: user 2026-09-01「案 a を採択して製造まで進めて」— worklist.py フル較正掃引
> (第 5 掃引・EXP-20260831-04 第 6 回観測)の起票範囲裁定。案 b(縮退診断文の真因名指し・
> 桁上限拡張)は見送り(実害未実測 — §8.5 整合)。裁定が gate ① を兼ねる。

## 担当設備(equipment)

- 製造: requested/resolved `claude-fable-5`・Claude Code(Claude Agent SDK)・来歴 **self-reported**

## 0. 実測(起票根拠 — 第 5 掃引の所見)

capability defect 0(--selftest が W1〜W6 全種+PROMOTION DUE+除外領域を恒久被覆・DUE は
実運用 2 件で接地)。所見= boundary 2(ID 連番 2 桁・watch N 単桁の縮退境界 — 境界プローブで
警告つき非計上を実測)/ declaration 2(縮退診断文の真因非名指し / Q10 様式の限界宣言なし —
docstring 解析原則 6 項は部分宣言の弱形)。S3(warnings 0= 整合)= 条件付き適格
〔形式整合のみ・意味整合は対象外設計 — EXP-20260830-04 の片側性と同族〕。

## 1. 変更要求(製造対象 — 案 a のみ)

worklist.py docstring へ「検出力の限界」節 4 項を追加 — **掃引実測からの転記のみ・新規限界の
創作なし**(裁定 5 前例= ui-cad-gate/ECO-045 と同じ「掃引後確定」の運び): (1) 形式整合のみ・
意味整合は対象外(structured 片側= EXP-20260830-04 引用)(2) 縮退境界(連番 2 桁・N 単桁 —
警告つき非計上・真因非名指し)(3) 除外領域の実記帳は沈黙(宣言済み境界の再掲)
(4) 常設対照の所在と DUE の実運用接地。**解析ロジック・selftest・出力は一切変更しない**。
案 b は見送り(実害= 3 桁到達・2 桁 N の実発生が出たら別 ECO)。

## 2. 影響なし予測(反証可能・製造前に凍結)

diff は worklist.py の docstring 追記(挿入のみ)+台帳系。--selftest 判定不変・
worklist/self-conformance 全判定不変。C16 は本 order を required と判定する見込み —
receipt 埋め込み済み。

## 3. 受入

- **V1**: 追記が掃引実測の転記のみ(各項が第 6 回観測・境界プローブ・EXP-20260830-04 へ
  トレース可能・新規限界なし)。
- **V2**: 挿入のみ(削除 0)・`worklist.py --selftest` PASS・self-conformance 全 PASS。
- **V3**: CI 緑(headSha 照合)。**V4**: diff 窓。

## /converge receipt(起動経路: 人間裁定〔案 a 採択〕)

- **判定: 収束**(round 軌跡: 1→0→0)。round 1 = 1 件(限界 (4) に常設対照の所在を含める —
  限界宣言は「測っていない次元」だけでなく「何で較正されているか」への参照面を兼ねる形に固定)。
- 検証した主張: 掃引プローブ 5 本の実測(第 6 回観測)/ EXP-20260830-04 実文 /
  docstring 原則 3・6 の実文。
- 未収束事項: なし。

## 4. 製造と受入の実測(2026-09-01)

- diff 監査の窓: baseline `bb4a239`(起票直前 HEAD・起票+製造は同一窓)→ head は受入時に確定。
- 限界 4 項を docstring へ追記(挿入のみ)。V1= 各項のトレース先を §1 に記載どおり確認。
- V2 以降は §5 で確定。

### 較正 receipt(/calibrate 自己適用 — trigger ③: 検査文の変更。二軸)

- 査定した主張と判定:
  1. 「追記は検査文の過大主張を作らない」— **observed / 適格**(全項が限界= 縮小方向の宣言で
     あり掃引実測の転記 — Q1 の逆方向)。
  2. 「解析・判定は不変」— **observed / 適格**(挿入のみ+--selftest・全検査の判定不変 —
     §5 実測)。
  3. 「宣言の実効到達(次の読み手が限界を踏まえて使う)」— **unknown(理由コード: 未実行)**。
- 検出した計器欠陥: なし。検出力の限界: 本査定はコメント変更の妥当性のみ。

## 5. CI 実測 / 6. クローズ

(push 後に追記)
