---
name: converge
description: 設計・対策案の収束ループ。提示前に収束条件(DoD)を固定し、反証可能な主張を実測で裏取りし、敵対自問で安定するまで内部反復してから、収束 receipt 付きで提示する。機械オラクルが未整備の設計合成(裁定候補・対策仕様・アーキテクチャ案・process equipment 設計)が対象。機械受入を持つ是正実施には使わない(/eco-fix のプローブ→受入がその収束機構)。
---

# /converge — 設計収束ループ(提示前の実測裏取り+敵対自問)

正典: `{{METHOD}}/method/templates/product-profile/skills/converge.md`(本ファイルの配布元)。
関連: `{{METHOD}}/method/bomdd-playbook-v1.md` §4.4(検査の対照)・§8.5(境界統制)・§13(記録の経済)。

目的: 人間の往復を「AI の検証不足の検出」に使わせず、**人間の仕事を裁定だけに戻す**。
出自: 設計レビューが人間の中継で 8 往復を要し、差し戻しの大半が AI 自身でリポジトリ実測により
自力反証できた主張だった(初出= TimetableAdv ECO-023)。

## 自発起動契約(この節が正本)

設計合成タスク — 裁定候補(3 案比較 CAD 等)、対策・process equipment 仕様、スキーマ設計、
アーキテクチャ案 — を認識したら、**人間の呼び出しがなくても本手順を適用する**。
ハーネス私有の memory・注記は補助トリガーにすぎない。非 Claude ハーネスも AGENTS.md 経由で
本ファイルを読み、同じ契約に従う。

> 注意: 自発起動を散文で規定しても起動は保証されない(実測あり)。**起動したこと自体を成果物に
> 残す**(収束 receipt)ことで、非起動と「起動したが指摘ゼロ」を区別する。

## 適用範囲

- **使う**: 合否を機械が判定できない設計合成すべて(上記)。
- **使わない**: 機械受入がある是正実施(`/eco-fix`)、事務的クローズ(`/eco-accept`)、
  単純な事実照会。収束機構の二重化は工程の無駄。

## 手順

1. **収束条件(DoD)の固定** — 設計に着手する前に、議題に**該当する項目だけ**を選んで
   チェックリスト化し、以後変更しない。テンプレ(選択式):
   - process equipment・統治規則向け:
     - 各規則に「**必ず起きるイベント**」のアンカーがある(人の記憶・善意に依存しない)
     - 各規則に**実在確認済みの実装先**(ファイル・関数・行)がある
     - 各検査に「**その規則固有の理由で赤くなる** fixture」の方針がある(schema 不足の FAIL で覆い隠さない)
     - 全状態に「**誰が・いつ更新するか**」の所有者がある(陳腐化検査)
   - UI/CAD・一般アーキテクチャ向け:
     - 正本が一意である(派生 cache は導出+不一致検査で従属させる)
     - 凍結オラクル行・既裁定の**実文**と突合済みである(設計意図と混同しない)
     - 影響が行単位で列挙されている(固定オラクル・control plan・golden・両リポジトリ・両ハーネス)
   - 議題固有の条件があれば追加する。

2. **draft 作成**。

3. **主張の抽出と実測義務** — draft から反証可能な主張を全て列挙する。特に
   「存在しない/できない/唯一/必ず/失効する/所有者がいない」型は**全件、リポジトリ実測**
   (grep・read・git・実行)で裏取りする。裏取れない主張は「疑い(未検証)」へ降格して事実と分離する。
   **効果の予測は本手順では裏取れない** — 実測できるのは「存在する/しない」型であり、
   「この対策は効く」は疑いのまま提示し、測定は受入・EXP へ回す。

4. **敵対自問** — 各設計要素に対して:
   - このイベントが起きなかったら、この規則は発火するか?
   - この状態は誰が更新するか? 更新されなかったら次の読み手に何が見えるか?
   - 正本が 2 つになっていないか? 片方だけ読んで断定していないか?
   - 現在状態と履歴を同じフィールドに混ぜていないか?
   - 自分の設計の帰結を「構造的制約」と読み替えていないか?
   - **選択肢を落としていないか?** 基準線が主因と測っている軸に、別のレバーが無いか。
   - 下の失敗型カタログと 1 件ずつ照合したか?

5. **ループ** — 3→4 で新規指摘が出たら draft を修正して繰り返す。
   **2 周連続で新規指摘ゼロ**になったら提示へ。**上限 3 周** — 収束しない論点は
   「未収束」として列挙し、抱え込まずに提示する。

6. **提示(1 回)** — **最終候補・比較に必要な選択肢・未収束事項は必ず提示する。
   省略してよいのは検証の途中過程だけ**。DoD チェックリストに ✔/✘ を付け、実測証拠は
   `file:line` で示し、人間の判断が要る点を質問として明示する。収束 receipt を添付する。

## 収束 receipt(提示物に添付)

- 周回数と各周の新規指摘件数
- 検証した主張と実測結果(要点のみ・`file:line` または実行コマンド)
- 未収束事項(なければ「なし」と明記)

提示物の中の一節として埋め込む(裁定候補なら decision 文書内、ECO なら本文内)。
新しい artifact 置場は作らない。

## 既知の失敗型 A: 汎用型(敵対自問時に必ず照合する)

> **本表は方法論側の正本**(`{{METHOD}}` 配下)。製品リポでの追記は下の**表 B** へ行う。

| # | 型 | 説明 | 初出 |
|---|---|---|---|
| ① | 実測せずの構造的断定 | 「〜できない/〜が唯一/〜に所有者がいない」を、実測せず構造の帰結として断定する。実際は運用未定義・経路が複数・数行下に規定あり、等 | TimetableAdv ECO-023 |
| ② | 検索を最初のヒット・停止行で打ち切る | grep の途中で読むのをやめ、直後の行にある反証規定を見落とす | TimetableAdv ECO-023 |
| ③ | 設計意図と凍結行の実文の混同 | 「意図はこうだった」を凍結済みオラクル行・裁定の要求そのものとして扱う。凍結行の**実文**を読み直していない | TimetableAdv ECO-023 |
| ④ | 開いているゲートの前提を再評価しない | 上流の裁定・封緘が既存の候補を失効させたのに、手続きの順序を優先して古い前提で進む | TimetableAdv ECO-023 |
| ⑤ | 台帳の一語を一次文書より優先 | 台帳のステータス語(`pending` 等)を、同時に読んでいた起票理由・一次記録より信じる | TimetableAdv ECO-023 |
| ⑥ | 検出範囲と権限範囲の混同 | 「検査器の保護パス外だから起票不要」と判定する。**検査器が捕捉しないことは許可ではない** | TimetableAdv ECO-023 |
| ⑦ | 選択肢集合の欠落 | 主張は正しいが、比較に必要な選択肢を提示しない。基準線が主因と測っている軸に別のレバーがあるのに載せない | BomDD 2026-08-28 |

## 既知の失敗型 B: 本リポ固有(製品リポが所有する追記領域)

> 学習ループ(下)の追記先は**この表**。汎用化して表 A へ戻すのは `/lesson-promote` の仕事であり、
> 本リポで表 A を書き換えない(配布元と分岐すると、どちらかが必ず腐る)。

| # | 型 | 説明 | 出典 ECO |
|---|---|---|---|
| — | (未記入) | | |

## 学習ループ(停止点)

提示後に人間(または外部レビュー)から差し戻しが来たら、指摘を上の失敗型へ分類する。

- **既知の型**なら手順 3〜4 の実施漏れ。原因をその周回の記録とともに報告する。
- **新しい型**なら**表 B へ追記を提案する** — 追記の実施も ECO 経由(⑥のとおり、
  機械検出されないことは許可ではない)。
- 表 B の型が**製品名を落としても成立する**と判断できたら、`/lesson-promote` で方法論側へ
  昇格を提案する(表 A への反映は方法論リポの ECO)。
